{
  "name": "claude-usage-widget",
  "version": "2.0.0",
  "private": true,
  "description": "SwiftBar plugin that shows Claude Code usage (session / weekly limits) in the macOS menu bar",
  "scripts": {
    "test": "bun test backend/lib/",
    "install-widget": "./install.sh",
    "uninstall-widget": "./uninstall.sh"
  },
  "license": "MIT"
}
